#include <stdio.h>
#include <unistd.h>
/*
int main(){
    pid_t pid = 0;
    long count = 0;

    while(pid != -1){
        pid = fork();
        if(pid != -1 && pid != 0){
            printf("%d, pid: %d, ppid: %d\n", ++count, pid, getppid());
        }
    }
}
*/
